<h1>Team Leaderboard</h1>

<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Rank</th>
    </tr>
  </thead>
  <tbody>
  <% @teams.each do |team| %>
    <tr>
      <td><%= link_to team.name, edit_league_team_path(@league, team) %></td>
      <td>??????</td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<a href="<%= new_league_team_path %>" button type="button" class="btn btn-primary">Join the League</a>
